spring:
  datasource:
    url: jdbc:mysql://localhost:3376/moovsmartTest?createDatabaseIfNotExist=true&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    username: root
    password: test1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: on
  mail:
    host: smtp.gmail.com
    port: 587
    username: application.demo7@gmail.com
    password: jlqujcvafcukqntg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

#  spring.mail.scheduler.persistence.enabled: false
#  spring.mail.scheduler.persistence.redis.embedded: false
#  spring.mail.scheduler.persistence.redis.enabled: false

